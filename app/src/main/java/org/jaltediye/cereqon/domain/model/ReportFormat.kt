package org.jaltediye.cereqon.domain.model

/**
 * Supported local report export formats persisted in Room [Report.format].
 */
object ReportFormat {
    const val PDF = "pdf"
    const val CSV = "csv"
}
